# DailyQT 태그 시스템 V2 — 하드 필터 + 소프트 가중치

## 핵심 원칙

```
모든 데이터는 2종류로만 존재한다:

1. 하드 필터 (Hard Filter)
   → 일치/불일치. 문자열 또는 boolean.
   → SQL WHERE 절로 후보군을 먼저 잘라냄.
   → "이건 아예 보여주면 안 됨" vs "후보에 포함"

2. 소프트 가중치 (Soft Weight)
   → 0~100 정수. 강도/정도를 나타냄.
   → 후보군 안에서 점수를 매겨 순위를 정함.
   → "이게 더 적합함" vs "저게 더 적합함"
```

---

## PART A: 하드 필터 (Hard Filters)

후보군을 추리는 데 사용. 점수가 아님. 일치/불일치만.

### A1. 성숙도 필터
```
maturity_level: "exploring" | "growing" | "close" | "centered"
```
- 사용자: 온보딩에서 1개 선택
- 콘텐츠: 각 블록에 허용 성숙도 배열 부여
- 규칙: `사용자.maturity_level IN 콘텐츠.allowed_maturity[]`
- 예시: 입문자에게 심화 해설 차단

### A2. 교회력/절기 필터
```
liturgical_season: "advent" | "christmas" | "epiphany" | "lent" | 
                   "holy_week" | "easter" | "pentecost" | "ordinary"
```
- 시스템이 오늘 날짜로 자동 계산
- 콘텐츠: 각 블록에 `season_tags[]` 부여 (해당 절기 전용 or 범용)
- 규칙: 절기 전용 콘텐츠 → 해당 절기에만 노출
         범용 콘텐츠 → 항상 후보에 포함
- 예시: 사순절 전용 해설은 사순절에만 나옴

### A3. 성경 메타 필터
```
testament: "OT" | "NT"
book: "창세기" | "시편" | ... | "요한계시록"  (66개 문자열)
chapter: 정수
verse_start: 정수
verse_end: 정수
genre: "율법" | "역사" | "시가" | "예언" | "복음" | "서신" | "묵시"
```
- 콘텐츠에 고정. 검색/필터용.
- 점수를 매기는 게 아님. "마태복음이 80점"은 성립 안 함.

### A4. 최근 노출 필터
```
recently_shown: boolean (최근 N일 내 이 블록을 봤는가)
```
- 규칙: 최근 7일 내 동일 본문 → 후보에서 제외
         최근 3일 내 동일 해설 → 후보에서 제외
- 반복 방지용 하드 차단.

### A5. 위기 플래그
```
is_crisis: boolean
```
- 콘텐츠: 특정 블록이 위기 상황(학대, 자살, 중독)에만 노출해야 할 때
- 사용자: 위기 상황 태그가 활성화된 사용자에게만 매칭
- 규칙: `is_crisis=true` 블록은 해당 상황 사용자에게만


---

## PART B: 소프트 가중치 (Soft Weights, 0~100)

하드 필터를 통과한 후보군 안에서 점수를 매기는 데 사용.

### B1. 영적 기질 (9개)
출처: 온보딩 질문 3-4개
변동: 거의 없음 (반년에 1번 재측정 제안)

```
T01  0~100  # 자연주의자
T02  0~100  # 감각주의자
T03  0~100  # 전통주의자
T04  0~100  # 금욕주의자
T05  0~100  # 행동주의자
T06  0~100  # 돌봄주의자
T07  0~100  # 열정주의자
T08  0~100  # 관상주의자
T09  0~100  # 지성주의자
```

### B2. 감정 상태 (15개)
출처: 일일 감정 카드 터치 (선택적)
변동: 매일. 터치 시 +80~100. 시간 감쇠 매일 ×0.7

#### 부정 감정 (10개)
```
E01  0~100  # 불안/초조
E02  0~100  # 슬픔/상실
E03  0~100  # 우울/무기력
E04  0~100  # 분노/억울
E05  0~100  # 외로움/단절
E06  0~100  # 죄책감/수치심
E07  0~100  # 스트레스/번아웃
E08  0~100  # 혼란/방향상실
E09  0~100  # 거절감/배신감
E10  0~100  # 허무/회의
```

#### 긍정 감정 (5개)
```
E11  0~100  # 감사/기쁨
E12  0~100  # 평안/안식
E13  0~100  # 소망/기대
E14  0~100  # 사랑/따뜻함
E15  0~100  # 경외/감동
```

시간 감쇠:
```
터치한 날:  90
+1일:      63 (×0.7)
+2일:      44
+3일:      31
+5일:      15
+7일:       7 → 실질적 소멸
```

### B3. 삶의 상황 압박도 (12개)
출처: 온보딩 프로필 + 간헐적 체크인 + 마이크로 질문
변동: 사용자 수동 변경 또는 마이크로 질문 응답 시. 시간 감쇠 ×0.85/일

#### 전통적 상황 압박 (8개)
```
P01  0~100  # 관계 갈등 압박도
P02  0~100  # 가족/양육 압박도
P03  0~100  # 직장/학업 압박도
P04  0~100  # 재정 압박도
P05  0~100  # 건강 압박도
P06  0~100  # 상실/애도 압박도
P07  0~100  # 환경변화 압박도
P08  0~100  # 중독/트라우마 압박도
```

#### 현대 사회 특수 압박 (4개)
```
P09  0~100  # 디지털 피로/비교의식 — SNS 비교, 둠스크롤링, FOMO
P10  0~100  # 성과주의 압박 — 끊임없는 증명, 계층 고착화, 미래 막막함
P11  0~100  # 연결 속 단절 — 군중 속 고독, 이념/세대 갈등, 에코체임버
P12  0~100  # 제도적 환멸/영적 고갈 — 교회 피로, 가나안 성도, 사역 번아웃
```

이건 "직장인이냐 아니냐(이진)"가 아니라
"직장 스트레스가 얼마나 심한가(정도)"를 나타냄.

P09~P12는 2026년 한국 사회 특수성 반영:
  P09 높으면 → 디지털 금식, 하나님 앞 정체성 묵상 매칭
  P10 높으면 → 은혜/무조건적 수용, 세상 기준 뒤집기 매칭
  P11 높으면 → 진정한 공동체, 원수 사랑, 화해 매칭
  P12 높으면 → 예수님과의 개인적 친밀함, 율법 아닌 은혜 매칭

### B4. 신학적 주제 (20개)
출처: 콘텐츠에 사전 부여 + 사용자 이력 축적
변동: 콘텐츠는 고정. 사용자는 QT 완료할 때마다 미세 축적.

```
TH01  0~100  # 하나님의 사랑/자비
TH02  0~100  # 하나님의 주권/섭리
TH03  0~100  # 하나님의 공의/심판
TH04  0~100  # 하나님의 임재/동행
TH05  0~100  # 십자가/대속/은혜
TH06  0~100  # 부활/영생/소망
TH07  0~100  # 회개/용서(받는것)
TH08  0~100  # 용서(주는것)/화해
TH09  0~100  # 기도/예배/찬양
TH10  0~100  # 말씀/진리/지혜
TH11  0~100  # 순종/제자도/헌신
TH12  0~100  # 성령/영적전쟁/능력
TH13  0~100  # 이웃사랑/섬김/긍휼
TH14  0~100  # 가정/결혼/관계
TH15  0~100  # 겸손/인내/절제
TH16  0~100  # 기쁨/감사/만족
TH17  0~100  # 평화/안식/쉼
TH18  0~100  # 전도/선교/사명
TH19  0~100  # 교회/공동체/연합
TH20  0~100  # 정의/돌봄/세계관
```

### B5. 콘텐츠 분위기 (4개)
콘텐츠 블록 자체의 톤/분위기 점수.

```
C01  0~100  # 위로/공감 정도 (높을수록 따뜻)
C02  0~100  # 도전/촉구 정도 (높을수록 날카로움)
C03  0~100  # 학문/분석 정도 (높을수록 지적)
C04  0~100  # 관상/침묵 정도 (높을수록 고요)
```

→ 기질과 직접 매칭:
  지성주의자(T09) ↔ 학문/분석(C03)
  돌봄주의자(T06) ↔ 위로/공감(C01)
  관상주의자(T08) ↔ 관상/침묵(C04)
  행동주의자(T05) ↔ 도전/촉구(C02)

### B6. 본문 속성 (3개)
```
D01  0~100  # 난이도 (0=매우쉬움 100=매우어려움)
D02  0~100  # 감정강도 (0=차분 100=격렬/절규)
D03  0~100  # 분량 (0=짧음 100=김)
```


---

## 전체 태그 수 확인

### 하드 필터 (숫자 아님, WHERE 조건)
```
A1. maturity_level    : 문자열 1개
A2. liturgical_season : 문자열 1개
A3. 성경 메타         : 문자열 5개 + 정수 3개
A4. recently_shown    : boolean
A5. is_crisis         : boolean
```

### 소프트 가중치 (0~100 숫자)
```
B1. 영적 기질    :  9개  (T01~T09)
B2. 감정 상태    : 15개  (E01~E15)
B3. 상황 압박도  : 12개  (P01~P12)
B4. 신학적 주제  : 20개  (TH01~TH20)
B5. 콘텐츠 분위기:  4개  (C01~C04)
B6. 본문 속성    :  3개  (D01~D03)
────────────────────────
소프트 태그 총합: 63개
```


---

## 스코어링 공식

### Step 1: 하드 필터 (SQL WHERE)

```sql
SELECT * FROM content_blocks
WHERE maturity_level @> ARRAY[user.maturity]     -- 성숙도 일치
  AND (season_tags @> ARRAY[today.season] OR season_tags = '{}')  -- 절기 일치 or 범용
  AND id NOT IN (최근 7일 노출 ID 목록)            -- 반복 방지
  AND (is_crisis = false OR user.has_crisis = true) -- 위기 차단
```
→ 수천 개 → 100~200개로 축소

### Step 2: 소프트 스코어링 (가중치 내적)

```
SCORE = (기질 매칭 × 0.20)
      + (감정 매칭 × 0.30)    ← 오늘의 핵심
      + (상황 매칭 × 0.10)
      + (주제 매칭 × 0.15)
      + (분위기 매칭 × 0.15)
      + (난이도 적합 × 0.10)
      ─────────────────────
      합계 = 1.00
```

각 차원의 매칭 계산:
```
기질 매칭 = Σ(user.T[i] × content.T[i]) / (9 × 10000) × 100
감정 매칭 = Σ(user.E[i] × content.E[i]) / (15 × 10000) × 100
...
```

### Step 3: 페널티 (감점)

```
최근 3일 내 동일 주제(TH) 최고점 블록 사용됨: -20점
동일 날 같은 분위기(C) 블록 중복:            -10점
```

### Step 4: 최종 선택

```
후보 100개 → 스코어링 → 상위 5개 → 랜덤 1개 선택
```


---

## 계산 예시

### 사용자 프로필
```json
{
  "maturity": "growing",
  "temperament": {"T08": 60, "T09": 85},
  "emotion": {"E01": 75, "E07": 50},
  "pressure": {"P03": 80},
  "theme_history": {"TH02": 40, "TH09": 30}
}
```

### 해설 블록 A (욥기 — 지적/분석적)
```json
{
  "allowed_maturity": ["growing", "close", "centered"],
  "season_tags": [],
  "tags": {
    "T09": 85, "C03": 90,
    "E01": 70, "E10": 60,
    "P03": 50,
    "TH02": 90, "TH03": 70,
    "D01": 75
  }
}
```

### 해설 블록 B (빌립보서 — 따뜻한/위로)
```json
{
  "allowed_maturity": ["exploring", "growing"],
  "season_tags": [],
  "tags": {
    "T06": 70, "T08": 60, "C01": 85,
    "E01": 80, "E07": 70,
    "P03": 80,
    "TH09": 80, "TH17": 75,
    "D01": 30
  }
}
```

### 하드 필터
```
블록 A: maturity "growing" ∈ ["growing","close","centered"] → ✅ 통과
블록 B: maturity "growing" ∈ ["exploring","growing"] → ✅ 통과
```

### 소프트 스코어링

블록 A:
```
기질: (60×0 + 85×85)/(9×10000)×100×0.20 = 7225/90000×100×0.20 = 1.61
감정: (75×70 + 50×0)/(15×10000)×100×0.30 = 5250/150000×100×0.30 = 1.05
상황: (80×50)/(8×10000)×100×0.10 = 4000/80000×100×0.10 = 0.50
주제: (40×90)/(20×10000)×100×0.15 = 3600/200000×100×0.15 = 0.27
분위기: (85×0 + 0×90)... 기질T09↔C03 직접매칭 = 85×90/10000×0.15 = 1.15
난이도: |user적정(50)-content(75)|=25 → (100-25)/100×0.10 = 0.75
TOTAL_A = 1.61 + 1.05 + 0.50 + 0.27 + 1.15 + 0.75 = 5.33
```

블록 B:
```
기질: (60×60 + 85×0)/(9×10000)×100×0.20 = 3600/90000×100×0.20 = 0.80
감정: (75×80 + 50×70)/(15×10000)×100×0.30 = 9500/150000×100×0.30 = 1.90
상황: (80×80)/(8×10000)×100×0.10 = 6400/80000×100×0.10 = 0.80
주제: (30×80)/(20×10000)×100×0.15 = 2400/200000×100×0.15 = 0.18
분위기: T08(60)↔C01(85) = 60×85/10000×0.15 = 0.77
난이도: |50-30|=20 → (100-20)/100×0.10 = 0.80
TOTAL_B = 0.80 + 1.90 + 0.80 + 0.18 + 0.77 + 0.80 = 5.25
```

### 결과
```
블록 A: 5.33점
블록 B: 5.25점
→ 블록 A (욥기 지적 해설)가 약간 우세 → 이 사용자는 지성주의자(85)이므로 합리적
```

만약 이 사용자가 감정 체크인에서 [스트레스] 대신 [외로워요]를 눌렀다면:
```
E05(외로움)=90 → 블록 B의 C01(위로)=85와 강하게 매칭
→ 블록 B가 역전 → 빌립보서 따뜻한 해설이 선택됨
```

이것이 "같은 사람이라도 오늘의 감정에 따라 다른 QT를 받는" 진정한 개인화.
